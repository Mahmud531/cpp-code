#include <iostream>
void binarySearch()
{
    int low = 0, high = 1e5;
    int mid = (low + high) / 2;
}
int main()
{
    ios::sync_with_stdio;
    cin.tie(0);
    cout.tie(0);

    std::cout << "hello world";
    return 0;
}
